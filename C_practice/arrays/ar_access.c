#include  <stdio.h>

/**
 *program that accesses elements of an array
 *Return: Always 0(sucees)
 */

int main(void)
{
	int arr[5] = {10, 20, 30, 40, 50};

	printf("element at arr[3] is: %d\n", arr[3]);

	printf("element at arr[0] is: %d\n", arr[0]);

	printf("element at arr[4] is: %d\n", arr[4]);

	return (0);
}
